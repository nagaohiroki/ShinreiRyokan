#pragma once
/*
#ifdef USE_GAMEPAD

class JoyStick
{
private:
	//ジョイスティックの取得
	HRESULT setupJoystick(LPDIRECTINPUT8,HWND );
	//ジョイスティックの設定
	HRESULT setJoyState(LPDIRECTINPUTDEVICE8,HWND);
public:
	// ジョイスティックの状態を取得する
	DIJOYSTATE2	aJoystickState;	
	//コンストラクタ
	JoyStick();
	//デストラクタ
	virtual ~JoyStick();
	//キーを取得
	VOID getJoystick(int*,HWND,int*);
	//ジョイスティックの作成
	HRESULT Create( HWND , HINSTANCE );	

};
#endif
*/